process TEST = 
{
 include "FWCore/MessageLogger/data/MessageLogger.cfi"

 source = PoolSource { 
	 untracked vstring fileNames = {"file:STAMuons.root"}
	 untracked int32 maxEvents = -1
  	}

 include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"

 module GLBTrackMatchAnlzr = GLBTrackMatchAnalyzer 
  {
#	untracked string DataType = "RealData"
	untracked string DataType = "SimData"
	untracked string StandAloneTrackCollectionLabel = "StandAloneMuon"
	untracked string TkTracksCollectionLabel = "FinalFitWMaterial"
	untracked string MuonSeedCollectionLabel = "MuonSeed"
	double Chi2CutTrackMatcher = 1000.0
	untracked string rootFileName = "GLBTrackMatchAnalyzer.root"
   }

 path p = {GLBTrackMatchAnlzr}

}
